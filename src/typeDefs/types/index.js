const { userType } = require('./userType');
const { postType } = require('./postType');
const { commentType } = require('./commentType');

module.exports = {
  commentType,
  postType,
  userType,
};
